// função que soma 2 numeros
function soma(a: number, b: number): number {
  return a + b;
}

//Função que concatena duas strings e retorna uma string
function concaternar(a: string, b: string): string {
  return a + b;
}

// Função que verifica se um numero é maior que outro e retorna um booleano
function eMaiorQue(a: number, b: number): boolean {
  return a > b;
}
